import {
  SYMBOLS_FETCH
} from '../contstants/actionTypes'


export const symbolsFetch = ({ prop, value }) => {
  console.log(itemSelect)
  return {
    type: ITEM_SELECT,
    payload: { prop, value }
  };
};
